# Base URL of the Node API gateway.
# Use your machine's LAN IP (e.g. http://192.168.1.20:4000) when testing on a
# physical phone — "localhost" on a phone means the phone itself.
EXPO_PUBLIC_API_URL=http://localhost:4000
